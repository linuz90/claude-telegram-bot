# Stays in the repository root: Docker only reads .dockerignore from the root of
# the build context, which is the repository root even though the Dockerfile
# lives in deployment/.
.git
.github
node_modules
deployment
docs
.claude
.vscode
.env
.env.*
*.log
tasks
